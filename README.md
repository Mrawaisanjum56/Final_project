# Final_project
University Final Year project 

## Optional ML dependency
- Wheat quality grading uses MobileNetV2 when TensorFlow is installed.
- Install with: `pip install tensorflow`
- If TensorFlow is unavailable, the app falls back to a lightweight heuristic score so listing flows keep working.
